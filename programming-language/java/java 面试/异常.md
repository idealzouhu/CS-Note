```
public String testDegradeRule(@PathVariable("id") int id) {
    // 模拟可能抛出的异常
    if (new Random().nextInt(10) < 8) {
        throw new RuntimeException("Simulated exception");
    }
    return "Request success!" + id;
}
```



异常会中断后面 return 语句的运行